# This .env file is safe to commit for this take-home.
# It contains only public config (no secrets or API keys).

# Open-Meteo API base URL
OPEN_METEO_BASE_URL=https://api.open-meteo.com

# Server port
PORT=4000

# Node environment
NODE_ENV=development
